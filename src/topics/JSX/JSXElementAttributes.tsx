const JSXElementAttributes = () => {
  /**
   * Los elementos JSX pueden tener atributos , al igual que los elementos HTML.
   * Un solo elemento JSX puede tener muchos atributos, como en HTML
   */
  const someImg = (
    <img src="images/panda.jpg" alt="panda" width="500px" height="5000x" />
  );
  return someImg;
};

export default JSXElementAttributes;